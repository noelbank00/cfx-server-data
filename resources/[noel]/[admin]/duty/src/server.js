/*
let onDuty = [];

// this event will listen through both scopes for a trigger
RegisterNetEvent('admin:onDuty');
onNet("admin:onDuty", (id) => {
	let index = array.indexOf(id);
	
	if (index > -1) {
		array.splice(index, 1);
	}else{
		onDuty.push(id);
	}

	console.log(onDuty);
})*/
